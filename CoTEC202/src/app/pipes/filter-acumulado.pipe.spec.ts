import { FilterAcumuladoPipe } from './filter-acumulado.pipe';

describe('FilterAcumuladoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAcumuladoPipe();
    expect(pipe).toBeTruthy();
  });
});
