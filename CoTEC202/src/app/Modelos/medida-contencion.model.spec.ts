import { MedidaContencion } from './medida-contencion.model';

describe('MedidaContencion', () => {
  it('should create an instance', () => {
    expect(new MedidaContencion()).toBeTruthy();
  });
});
